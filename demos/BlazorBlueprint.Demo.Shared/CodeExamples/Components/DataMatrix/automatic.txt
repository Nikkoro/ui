@using BlazorBlueprint.Primitives.DataMatrix

<BbDataMatrix Value="@value" EncodingMode="DataMatrixEncodingMode.Auto"
              OnEncoded="@(symbol => current = symbol)" />

@code {
    private string value = "ABC123456789";
    private DataMatrixSymbol? current;
}
